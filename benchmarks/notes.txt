residual + layernorm

done:
    linear forward: QKV proj, out_proj W_o
    patch embedding
    layernorm
    scaled_qk - no linear_forward: K has B, H
    softmax
    attention @ v - no linear_forward: no mathematical V.T means V stored differently in mem
    fused_attention
    FlashAttention
    fc1 + bias + GELU



comparisons:
3 attention levels
warp patch embedding / attention_v vs not


Benchmark:
Correctness
median latency
speedup vs pytorch
scaling w/ input size
kernel launch count
peak mem usage
effective bandwidth / TFLOP/s
Nsight GPU eff metrics
Amdahls Law
